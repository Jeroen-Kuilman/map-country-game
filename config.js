export const config = Object.freeze({
  MIN_POP_SIZE: 50000,
  MAX_LIST_RESULTS: 10,
  UPDATE_ROUND_SECONDS: 0.6,
  PLAYER_WRONG_MAX: 10,
  PLAYER_CORRECT_MAX: 2,
  POINTS_INIT_VALUE: 0,
});

export const RESULT = Object.freeze({
  CORRECT: "correct",
  WRONG: "wrong",
});
